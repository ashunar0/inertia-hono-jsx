{
  "name": "inertia-hono-jsx-sample",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "$npm_execpath run build && vite preview",
    "deploy": "$npm_execpath run build && wrangler deploy",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@hono/inertia": "^0.2.0",
    "@ts-76/inertia-hono-jsx": "^0.1.0",
    "hono": "^4.12.16"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.2.3",
    "typescript": "^5.9.3",
    "vite": "^6.3.5",
    "vite-ssr-components": "^0.6.1",
    "wrangler": "^4.17.0"
  }
}
